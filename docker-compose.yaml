services:
  mongodb: # --name mongodb
    image: mongodb:v1


networks:
  default:
    driver: bridge
    name: roboshop
    external:false # true if u have created network manually using docker network command
